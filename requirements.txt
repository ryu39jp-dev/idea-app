# アイデア評価ループアプリ — 依存パッケージ
streamlit>=1.35.0
python-dotenv>=1.0.0
pandas>=2.0.0

# LLM プロバイダー（使う方だけインストールすればOK）
openai>=1.30.0           # OpenAI (gpt-4o-mini など)
 google-generativeai>=0.5.0  # Gemini を使う場合はこちらをコメントアウト解除